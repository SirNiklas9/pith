# built binary
/pith.exe

# personal, code-anchored work list (per-checkout, not shared)
.pith-work.json

# jetbrains plugin build artifacts
jetbrains/.gradle/
jetbrains/build/

# vscode extension build artifacts (binaries staged at package time)
vscode/bin/
vscode/node_modules/
vscode/*.vsix

# release staging
/release/
